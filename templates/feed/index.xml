<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xmlns:media="http://search.yahoo.com/mrss/" xml:lang="{{ lang }}">
    <title>{{ config.title }}</title>
    <subtitle>{{ config.description }}</subtitle>
    <link rel="self" type="application/atom+xml" href="{{ feed_url | safe }}"/>
    <link rel="alternate" type="text/html" href="{{ config.base_url | escape_xml | safe }}"/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    {% if last_updated is defined %}<updated>{{ last_updated | date(format="%Y-%m-%dT%H:%M:%S%:z") }}</updated>{% endif %}
    <id>{{ feed_url | safe }}</id>
    <rights>Copyright &#169; {{ now() | date(format="%Y") }}, {{ config.author }}</rights>
    {%- for page in pages %}
    <entry xml:lang="{{ page.lang }}">
        <title>{{ page.title }}</title>
        {% if page.date %}<published>{{ page.date | date(format="%Y-%m-%dT%H:%M:%S%:z") }}</published>
        <updated>{{ page.updated or page.date | date(format="%Y-%m-%dT%H:%M:%S%:z") }}</updated>{% endif %}
        <author>
          <name>{{ config.author }}</name>
          <email>{{ config.extra.author_email }}</email>
        </author>
        <link rel="alternate" type="text/html" href="{{ page.permalink | safe }}"/>
        <id>{{ page.permalink | safe }}</id>
        <media:thumbnail url="{{ get_url(path=page?.extra?.card or config.extra.default_card) | escape_xml | safe }}"/>
        {% if page.summary %}<summary type="html">{{ page.summary }}</summary>{% endif %}
        <content type="html" xml:base="{{ page.permalink | escape_xml | safe }}">{{ page.content }}</content>
    </entry>
    {%- endfor %}
</feed>
